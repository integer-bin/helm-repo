# Default values for custom-application.
# This is where you can set customizable values.

was:
  replicaCount: 3

nginx:
  replicaCount: 5

wasImage:
  repository: "integerbin1/cicd"
  tag: v3
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
# ingress:
#   enabled: true
#   annotations: {}
#   hosts:
#     - host: www.frodo.kr
#       paths: []
#   tls: []

